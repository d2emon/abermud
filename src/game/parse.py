def eorte():
    import logging
    logging.debug("---> eorte()")
    # extern long mynum,me_ivct;
    # extern long me_drunk;
    # extern long ail_dumb;
    # extern long curch,tdes,rdes,vdes,ades;
    # extern long me_cal;
    # extern long wpnheld;
    # extern long my_str;
    # extern long i_setup;
    # extern long interrupt;
    # extern long fighting,in_fight;

    # ctm = time()
    # if ctm - last_io_interrupt > 2:
    #    interrupt = 1

    # if interrupt:
    #     last_io_interrupt = ctm

    # if me_ivct:
    #     me_ivct -= 1

    # if me_ivct == 1:
    #     setpvis(mynum, 0)

    # if me_cal:
    #     me_cal = 0
    #     calibme()

    # if tdes:
    #     dosumm(ades)

    # if in_fight:
    #     if ploc(fighting) != curch:
    #         fighting = -1
    #         in_fight = 0
    #     if not strlen(pname(fighting)):
    #         fighting = -1
    #         in_fight = 0
    #     if in_fight:
    #         if interrupt:
    #             in_fight = 0
    #             hitplayer(fighting, wpnheld)

    # if iswornby(18,mynum) or randperc() < 10:
    #     my_str += 1
    #     if i_setup:
    #         calibme()

    # forchk()

    # if me_drunk > 0:
    #     me_drunk -= 1
    #     if not ail_dumb:
    #         gamecom("hiccup")

    # interrupt = 0
    pass
